MENSAGEM=$1

git commit -a -m "$MENSAGEM"; git push; curl http://producao.virtual.ufpb.br/cgi-bin/pull-pdf.py?repositorio=`git config --get remote.origin.url`; rm -rf livro.pdf; wget -nv -i producao-livro; play ~/beep.mp3
